WIN32 := 1

export PATH := $(MINGW_HOME)/mingw32/bin;$(MSYS_HOME)/usr/bin:$(PATH)

BIN_EXT := .exe

LIB_PREFIX := 

LIB_EXT := .lib

ifeq ($(origin CXX),default)
CXX := g++.exe
endif

ifeq ($(origin CC),default)
CC := gcc.exe
endif

AR := ar.exe

RM := rm.exe

CP := cp.exe

MKDIR := mkdir.exe
