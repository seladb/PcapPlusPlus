WIN32 := 1

export PATH := $(MINGW_HOME)/bin;$(MSYS_HOME)/bin:$(PATH)

BIN_EXT := .exe

LIB_PREFIX := 

LIB_EXT := .lib

ifeq ($(origin CXX),default)
CXX := g++.exe
endif

ifeq ($(origin CC),default)
CC := gcc.exe
endif

AR := ar.exe

RM := rm.exe

CP := cp.exe

MV := mv.exe

TOUCH := touch.exe

MKDIR := mkdir.exe
