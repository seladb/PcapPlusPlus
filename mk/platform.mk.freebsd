FREEBSD := 1

BIN_EXT :=

LIB_PREFIX := lib

LIB_EXT := .a

ifeq ($(origin CXX),default)
CXX := clang++
endif

ifeq ($(origin CC),default)
CC := clang
endif

AR := ar

RM := rm

CP := cp

MV := mv

TOUCH := touch

MAKE := gmake

MKDIR := mkdir

INSTALL_SCRIPT := install.sh

UNINSTALL_SCRIPT := uninstall.sh
